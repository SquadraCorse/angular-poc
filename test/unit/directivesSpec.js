'use strict';

/* jasmine specs for directives go here */


/*
describe("Lazy image", function() {

    var el, scope;

    beforeEach(module('my-app'));

    beforeEach(inject(function($compile, $rootScope) {

        scope = $rootScope.$new();
        el = angular.element('<div tif-lazy-image="foo.png"></div>');
        $compile(el)(scope);
        scope.$digest();

    }));


    it('does it have image attached', function() {
        
        expect(el.html()).toBe("");
    });


}); 

*/
